server:
  name: "aichatbot"
  description: "AI Chatbot Server"

telegram:
  token: "YOUR_TELEGRAM_BOT_TOKEN"

llm:
  # api_url: base URL for OpenAI-compatible providers, e.g. https://api.deepseek.com/v1
  # Leave commented out to use the official OpenAI API.
  api_url: ""
  model: "gpt-4o-mini"
  api_key: "YOUR_LLM_API_KEY"
  temperature: 0.7
  max_tokens: 1000

weather:
  api_key: "YOUR_OPENWEATHERMAP_API_KEY"
